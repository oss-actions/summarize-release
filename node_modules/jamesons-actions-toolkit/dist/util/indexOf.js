"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function indexOf(haystack, needle, startsAt = 0) {
    if (startsAt > 0)
        haystack = haystack.slice(startsAt);
    return typeof needle === "string"
        ? haystack.indexOf(needle, startsAt)
        : (startsAt && startsAt > 0 ? haystack.slice(startsAt) : haystack).search(needle);
}
exports.default = indexOf;
